{{> post-info post}}

{{#if loggedIn}}
<div class="container text-center bg-dark text-white mt-2 mb-2 pt-2 pb-1">
    <h3>Leave a Comment</h3>
        <div class="mb-3">
            <textarea class="form-control" name="comment-content" id="comment-content" rows="3" placeholder="Enter Comment"></textarea>
        </div>
        <button class="btn btn-light mb-2" id="add-comment">
            Add Comment
        </button>
</div>
{{/if}}

{{> comments post.comments}}

{{#if loggedIn}}
<script src="/js/comment.js"></script>
{{/if}}